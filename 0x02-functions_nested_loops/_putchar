#include <unistd.h>

/**
 * Descriptio: writes the character c to stdout
 * @c: The charactee to print
 *
 * Return: On success 1.
 * If error, -1 is returned
 */
int _putchar(char c) {
    // Write the character to stdout (file descriptor 1)
    return write(1, &c, 1);
}
