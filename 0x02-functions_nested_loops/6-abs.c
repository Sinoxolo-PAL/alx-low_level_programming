/** 
 * _abs - Computes the absolute value of an integer.
 *
 * @n: an integer.
 *
 * Return: the absolute value of an integer.
 */

int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
