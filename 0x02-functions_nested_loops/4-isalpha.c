#include <ctype.h>

/**
 * _isalpha - Checks if character is an alphabet
 *
 * @c: is the character input to check
 *
 * Return: 1 for alphabets and 0 otherwise
 */

int _isalpha(int c)
{
if isalpha(c)
{
return (1);
}
else
{
return (0);
}
}
