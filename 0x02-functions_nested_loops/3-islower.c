#include <ctype.h>

/**
 * _islower - Checks if character is in lowercase.
 *
 * @c: is the character at hand.
 *
 * Return: (0) if not lowercase otherwise (1).
 */

int _islower(int c)
{
if islower(c)
{
return (1);
}
else
{
return (0);
}
}
